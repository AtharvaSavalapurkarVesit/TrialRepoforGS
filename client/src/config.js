// Configuration settings for the application

// API URL - uses environment variable if available, falls back to localhost
export const API_URL = process.env.REACT_APP_API_URL || 'http://localhost:5002'; 